import { Time } from '@angular/common';

export interface Grupo {
    clave_materia?: string;
    clave_grupo?: string;
    clave_profesor?: string;
    hora_inicio?: string;
    hora_final?: string;
}